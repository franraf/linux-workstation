-- Keyboard, pointer and touchpad baseline.

hl.config({
  input = {
    kb_layout = "br",
    follow_mouse = 1,
    touchpad = {
      natural_scroll = false,
      disable_while_typing = true,
      tap_to_click = true,
    },
  },
})
